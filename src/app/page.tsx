export default function Home() {
  return (
    <main className="bootstrap-shell">
      <section>
        <p className="eyebrow">Sermon Studio</p>
        <h1>Repository initialized.</h1>
        <p>
          This static Next.js shell exists so the Rocket build can connect and replace/extend it while preserving the SermonBackend architecture.
        </p>
      </section>
    </main>
  );
}
